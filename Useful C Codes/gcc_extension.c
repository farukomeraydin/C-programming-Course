#define _CRT_SECURE_NO_WARNINGS
#include "Omer.h"

//With gcc expression statement extension safety problem is solved.

#define maxint(a,b) \
({int _a = (a), _b = (b); _a > _b ? _a : _b;})

#define gmax(a,b) \
({typeof(a) _a = (a); \
  typeof(b) _b = (b); \
  _a > _b ? _a : _b;})

int main()
{
	
}
